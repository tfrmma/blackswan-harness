fn main() {
    // TODO: real CLI (clap, scenario loading, run/replay subcommands) lands in
    // phase 5 once there's something to run.
    println!("blackswan-harness: skeleton only, nothing to run yet");
}
