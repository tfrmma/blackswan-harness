[package]
name = "blackswan-kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
build = "build.rs"

[dependencies]
blackswan-core = { path = "../core" }
aya = "0.12"
libc = "0.2"

[dev-dependencies]
blackswan-replay = { path = "../replay" }
