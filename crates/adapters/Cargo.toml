[package]
name = "blackswan-adapters"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
blackswan-core = { path = "../core" }

[dev-dependencies]
blackswan-replay = { path = "../replay" }
